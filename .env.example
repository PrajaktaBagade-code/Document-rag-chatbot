# Choose "openai" or "mistral" (mistral is free via HuggingFace Inference API)
LLM_PROVIDER=mistral

# Required only if LLM_PROVIDER=openai
OPENAI_API_KEY=sk-...

# Required only if LLM_PROVIDER=mistral (free token from huggingface.co/settings/tokens)
HUGGINGFACEHUB_API_TOKEN=hf_...
